CREATE TABLE obtained_consent (
 id TEXT NOT NULL PRIMARY KEY
);

selectAll:
SELECT *
FROM obtained_consent;

insert:
INSERT OR REPLACE INTO obtained_consent(id)
VALUES (?);

removeAll:
DELETE FROM obtained_consent;
