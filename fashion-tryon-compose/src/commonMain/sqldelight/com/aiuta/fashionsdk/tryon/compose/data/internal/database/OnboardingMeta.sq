CREATE TABLE onboarding_meta (
    id TEXT NOT NULL PRIMARY KEY
);

select:
SELECT *
FROM onboarding_meta
LIMIT 1;

count:
SELECT count(*)
FROM onboarding_meta;

insert:
INSERT OR REPLACE INTO onboarding_meta(id)
VALUES (?);

removeAll:
DELETE FROM onboarding_meta;
